export const SCREEN_WIDTH = window.innerWidth;
export const SCREEN_HEIGHT = window.innerHeight;

export const DEFAULT_X_VELOCITY = 350;
export const DEFAULT_Y_VELOCITY = 800;

export const TILE_SIZE = 128;
